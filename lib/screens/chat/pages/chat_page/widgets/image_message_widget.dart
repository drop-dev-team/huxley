import 'package:flutter/material.dart';

class ImageMessage extends StatelessWidget {
  const ImageMessage({super.key, required String currentUserId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
