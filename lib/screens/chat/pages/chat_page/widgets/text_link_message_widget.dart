import 'package:flutter/material.dart';

class TextLinkMessage extends StatelessWidget {
  const TextLinkMessage({super.key, required String currentUserId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
