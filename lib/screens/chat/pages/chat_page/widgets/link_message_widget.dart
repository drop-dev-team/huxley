import 'package:flutter/material.dart';

class LinkMessage extends StatelessWidget {
  const LinkMessage({super.key, required String currentUserId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
