import 'package:flutter/material.dart';

class ImageLinkMessage extends StatelessWidget {
  const ImageLinkMessage({super.key, required String currentUserId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
