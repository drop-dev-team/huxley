import 'package:flutter/material.dart';

class TextImageMessage extends StatelessWidget {
  const TextImageMessage({super.key, required String currentUserId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
